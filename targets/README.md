# Targets

Place compiled MindAR image target files here.

Expected demo paths:

- `demo-image.mind`
- `demo-model.mind`
- `demo-video.mind`

Every time the printed target image changes, recompile the matching `.mind` file before testing on phones.
